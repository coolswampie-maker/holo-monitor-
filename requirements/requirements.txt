# ГОЛОЦИТ — зависимости с ЖЁСТКОЙ фиксацией версий.
#
# Версии зафиксированы намеренно. Обученные модели сохраняются средствами
# joblib, а формат этих файлов несовместим между версиями scikit-learn и
# numpy. Собранная под одни версии модель на других просто не загрузится.
# Поэтому среда обучения и среда у заказчика обязаны совпадать.
#
# При смене любой версии ниже модели нужно переобучить:
#     python3 scripts/train_models.py
#
# Все пакеты ставятся из готовых колёс, сборка из исходников не требуется.

numpy==2.4.6
scipy==1.17.1
scikit-image==0.26.0
scikit-learn==1.9.1
matplotlib==3.11.2
tifffile==2026.3.3
Pillow==12.3.0
joblib==1.6.0

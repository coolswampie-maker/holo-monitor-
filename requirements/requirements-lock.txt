# ГОЛОЦИТ — production-зависимости с точной фиксацией.
#
# Версии зафиксированы намеренно. Обученные модели сохраняются
# средствами joblib, а формат этих файлов несовместим между версиями
# scikit-learn и numpy: собранная под одни версии модель под другими
# не загрузится вовсе. Среда обучения и среда у заказчика обязаны
# совпадать, поэтому здесь ==, а не >=.
#
# Целевая среда: Python 3.11 x64.
# При смене любой версии переобучить модели:
#     python scripts/train_models.py

numpy==2.4.6
scipy==1.17.1
scikit-image==0.26.0
scikit-learn==1.9.1
matplotlib==3.11.2
tifffile==2026.3.3
Pillow==12.3.0
joblib==1.6.0
